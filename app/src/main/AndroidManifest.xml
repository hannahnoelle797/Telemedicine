<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.telemedicine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.scheduling.ApptSchedulingFirebase"></activity>
        <activity
            android:name=".ui.signup.Signup"
            android:label="Sign up"></activity>
        <activity
            android:name=".ui.login.Login"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".ui.sample_pages.sample_report"
            android:label="ReportsActivity"></activity>
        <activity
            android:name=".ui.scheduling.appointmentScheduling"
            android:label="AppointmentScheduling"></activity>
    </application>

</manifest>